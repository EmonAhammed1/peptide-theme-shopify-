{% comment %} Age Gate — Full-screen overlay, blocks all site access {% endcomment %}

<div id="age-gate-overlay" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:#080B10; z-index:99999; overflow-y:auto;">
  <div style="min-height:100vh; display:flex; align-items:center; justify-content:center; padding:40px 20px;">
    <div style="max-width:520px; width:100%; text-align:center;">

      <!-- Store Name -->
      <div style="font-family:'JetBrains Mono',monospace; font-size:13px; font-weight:800; color:#3cd6c4; text-transform:uppercase; letter-spacing:0.2em; margin-bottom:28px;">
        {{ shop.name | upcase }}
      </div>

      <!-- Label -->
      <div style="font-family:'JetBrains Mono',monospace; font-size:10px; font-weight:700; color:#6b7280; text-transform:uppercase; letter-spacing:0.18em; margin-bottom:16px;">
        // AGE VERIFICATION
      </div>

      <!-- Heading -->
      <h1 style="font-family:'Space Grotesk',sans-serif; font-size:clamp(26px,5vw,36px); font-weight:800; color:#ffffff; margin:0 0 20px 0; line-height:1.2;">
        You Must Be 21 or Older
      </h1>

      <!-- Description -->
      <p style="font-family:'Space Grotesk',sans-serif; font-size:14px; color:#8a929e; line-height:1.7; margin:0 auto 40px; max-width:400px;">
        This site is restricted to qualified researchers and adults aged 21 and over.<br>
        By entering you confirm you meet these requirements.
      </p>

      <!-- Enter Button -->
      <button
        onclick="ageGateConfirm()"
        style="display:block; width:100%; padding:18px 24px; background:#3cd6c4; color:#000000; border:none; border-radius:4px; font-family:'JetBrains Mono',monospace; font-size:12px; font-weight:800; text-transform:uppercase; letter-spacing:0.12em; cursor:pointer; transition:all 0.3s ease; margin-bottom:18px;"
        onmouseover="this.style.background='#2eb8a8'"
        onmouseout="this.style.background='#3cd6c4'"
      >
        I AM 21 OR OLDER — ENTER SITE
      </button>

      <!-- Leave Link -->
      <a
        href="https://www.google.com"
        style="display:block; font-family:'JetBrains Mono',monospace; font-size:10px; color:#4b5563; text-decoration:none; text-transform:uppercase; letter-spacing:0.1em; transition:color 0.3s ease;"
        onmouseover="this.style.color='#6b7280'"
        onmouseout="this.style.color='#4b5563'"
      >
        I AM UNDER 21 — LEAVE
      </a>

    </div>
  </div>
</div>

<script>
  (function() {
    var confirmed = localStorage.getItem('age-gate-confirmed');
    if (!confirmed) {
      var overlay = document.getElementById('age-gate-overlay');
      if (overlay) {
        overlay.style.display = 'block';
        document.body.style.overflow = 'hidden';
      }
    }
  })();

  function ageGateConfirm() {
    localStorage.setItem('age-gate-confirmed', 'true');
    var overlay = document.getElementById('age-gate-overlay');
    if (overlay) {
      overlay.style.opacity = '1';
      overlay.style.transition = 'opacity 0.5s ease';
      setTimeout(function() {
        overlay.style.opacity = '0';
        setTimeout(function() {
          overlay.style.display = 'none';
          document.body.style.overflow = '';
        }, 500);
      }, 10);
    }
  }
</script>

{% schema %}
{
  "name": "Age Gate",
  "settings": [],
  "presets": [
    {
      "name": "Age Gate"
    }
  ]
}
{% endschema %}
